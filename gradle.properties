# ============================================
# 🚀 SOTA-Level Gradle Performance Optimization
# ============================================
# 
# 이 설정은 빌드 속도를 최대한 높이기 위한 SOTA급 최적화입니다.
# 일반적인 빌드 대비 3-5배 빠른 성능을 기대할 수 있습니다.
#

# ============================================
# 1. JVM 최적화 (Gradle Daemon)
# ============================================

# JVM 메모리 및 GC 최적화
# - Xmx6g: 빌드용 최대 힙 메모리 6GB
# - XX:+UseG1GC: 대용량 힙에 최적화된 G1 GC
# - XX:MaxGCPauseMillis=200: GC 일시정지 최소화
# - XX:+UseStringDeduplication: 문자열 중복 제거로 메모리 절약
# - XX:SoftRefLRUPolicyMSPerMB=50: 소프트 레퍼런스 공격적 정리
org.gradle.jvmargs=-Xmx6g \
  -XX:+UseG1GC \
  -XX:MaxGCPauseMillis=200 \
  -XX:+UseStringDeduplication \
  -XX:SoftRefLRUPolicyMSPerMB=50 \
  -XX:+HeapDumpOnOutOfMemoryError \
  -XX:MaxMetaspaceSize=1g \
  -Dfile.encoding=UTF-8 \
  -Dkotlin.daemon.jvm.options=-Xmx3g

# ============================================
# 2. 병렬 처리 (Parallelism)
# ============================================

# 병렬 빌드 활성화 (멀티코어 100% 활용)
org.gradle.parallel=true

# 병렬 워커 수 (CPU 코어 수에 맞게 자동 조절, 수동 설정 시 아래 주석 해제)
# org.gradle.workers.max=8

# Gradle 데몬 (JVM 재시작 방지 - 필수)
org.gradle.daemon=true

# 데몬 유휴 타임아웃 (3시간 - 개발 중 데몬 유지)
org.gradle.daemon.idletimeout=10800000

# ============================================
# 3. 캐싱 (Caching) - 가장 중요!
# ============================================

# Configuration Cache (설정 단계 캐싱 - 매우 효과적)
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# Build Cache (태스크 결과 캐싱)
org.gradle.caching=true

# 파일 시스템 감시 (증분 빌드 정확도 향상)
org.gradle.vfs.watch=true
org.gradle.vfs.verbose=false

# ============================================
# 4. 태스크 실행 최적화
# ============================================

# 필요한 프로젝트만 설정 (멀티모듈에서 효과적)
org.gradle.configureondemand=true

# 의존성 락 (reproducible builds)
# org.gradle.dependency.locking.enabled=true

# ============================================
# 5. Kotlin 컴파일러 SOTA 최적화
# ============================================

# Kotlin 코드 스타일
kotlin.code.style=official

# 증분 컴파일 (변경된 파일만 재컴파일)
kotlin.incremental=true

# JVM IR 백엔드용 증분 컴파일
kotlin.incremental.useClasspathSnapshot=true

# 정밀한 Java 증분 컴파일 추적
kotlin.incremental.usePreciseJavaTracking=true

# 병렬 Kotlin 컴파일 (프로젝트 내 병렬화)
kotlin.parallel.tasks.in.project=true

# Kotlin 컴파일러 캐시
kotlin.caching.enabled=true

# Kotlin 데몬 JVM 설정 (별도 프로세스)
kotlin.daemon.jvmargs=-Xmx3g -XX:+UseG1GC -XX:MaxGCPauseMillis=100

# Kotlin 컴파일러 데몬 유지
kotlin.daemon.useFallbackStrategy=false

# ============================================
# 6. Kapt (Annotation Processing) 최적화
# ============================================

# Kapt 증분 처리
kapt.incremental.apt=true

# Kapt 워커 API 사용 (병렬 처리)
kapt.use.worker.api=true

# Kapt 컴파일 회피 (가능한 경우 스킵)
kapt.include.compile.classpath=false

# Kapt 정확한 증분 처리
kapt.useBuildCache=true

# ============================================
# 7. 콘솔 & 로깅 최적화
# ============================================

# 콘솔 출력 최적화 (rich console 대신 plain)
# org.gradle.console=plain

# 경고 모드 (none으로 설정하면 출력 감소)
org.gradle.warning.mode=summary

# 빌드 스캔 비활성화 (속도 향상)
# org.gradle.scan.enabled=false

# ============================================
# 8. 네트워크 & 의존성 최적화
# ============================================

# 오프라인 모드 (네트워크 요청 스킵 - 필요 시 활성화)
# org.gradle.offline=true

# 의존성 검증 스킵 (개발용)
# org.gradle.dependency.verification=off

# ============================================
# 프로젝트 정보 (Maven 배포용)
# ============================================
group=com.oliveyoung
# version은 src/main/kotlin/com/oliveyoung/ivmlite/sdk/VERSION 파일에서 읽음
